import React from 'react';


const ColorSelectorCircle = (props) => {
    return (
        <div className="SelectorCircle"  onClick={() => props.handleColorSelect()}>
           
        </div>
    )
}

export default ColorSelectorCircle;